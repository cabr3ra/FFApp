<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FFApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/red_origin</item>
        <item name="colorPrimaryVariant">@color/red_origin</item>
        <item name="android:statusBarColor">@color/red_origin</item>
    </style>

    <style name="Theme.FFApp" parent="Base.Theme.FFApp" />
</resources>